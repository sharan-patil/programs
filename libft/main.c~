#include <libft.h>

int main(void)
{
	int i = 0;
    char arr[] = "split  ||this|for|me|||||!";
    char **lmao;
    lmao = ft_strsplit(arr, '|');
    while (i < 10)
    {
    	printf("%s\n", lmao[i]);
    	i++;
    }
    return (0);
}
